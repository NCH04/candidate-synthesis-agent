name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend — lint + tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        # sentence-transformers/torch is intentionally absent: skill_match_service
        # imports it lazily, so the suite runs without a 2 GB download.
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements-dev.txt

      - name: Lint
        run: ruff check .

      - name: Tests
        env:
          DEMO_MODE: "true"
          ANTHROPIC_API_KEY: "not-used-in-tests"
        run: pytest -q

  frontend:
    name: Frontend — typecheck + build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci --no-audit --no-fund

      - name: Typecheck
        working-directory: frontend
        run: npx tsc --noEmit

      - name: Build
        working-directory: frontend
        run: npm run build

      - name: Check the bundled fixtures match the backend
        # frontend/src/generated/ is committed because Rollup resolves the
        # staticDemo import on every build path. Fail loudly if it drifts from
        # the backend, which stays the source of truth.
        run: |
          diff backend/demo_data/sample.json frontend/src/generated/demo-sample.json \
            || { echo "::error::demo-sample.json is stale — run ./scripts/build-static-demo.sh and commit"; exit 1; }
          diff backend/jobs.json frontend/src/generated/jobs.json \
            || { echo "::error::jobs.json is stale — run ./scripts/build-static-demo.sh and commit"; exit 1; }

      - name: Build the static demo bundle
        # Guards the public demo: a change that breaks the bundle fails here.
        run: ./scripts/build-static-demo.sh

  docker:
    name: Docker image builds
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Build image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
