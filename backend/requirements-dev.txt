# Test + lint dependencies.
# Deliberately does NOT pull sentence-transformers/torch (~2 GB): the embedding
# model is imported lazily, so the suite and CI run without it.
-r requirements.txt
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
ruff>=0.5.0
